rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // V1 nu păstrează fotografiile problemelor în Cloud Storage.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
